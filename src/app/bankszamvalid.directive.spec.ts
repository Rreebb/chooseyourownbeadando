import { BankszamvalidDirective } from './bankszamvalid.directive';

describe('BankszamvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new BankszamvalidDirective();
    expect(directive).toBeTruthy();
  });
});
